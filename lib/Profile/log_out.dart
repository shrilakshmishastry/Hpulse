import 'package:flutter/material.dart';

//class to show logout button if user has logged in
class LogOut extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("logout"),
    );
  }
}
